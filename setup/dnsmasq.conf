# DHCP only. port=0 disables dnsmasq's DNS server so it does not fight
# systemd-resolved over port 53.
# Run: sudo dnsmasq -C setup/dnsmasq.conf -d
interface=wlp3s0
bind-interfaces
port=0
dhcp-range=192.168.50.10,192.168.50.100,12h
dhcp-option=3,192.168.50.1
dhcp-option=6,8.8.8.8,1.1.1.1
